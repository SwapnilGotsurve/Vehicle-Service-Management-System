import { useEffect, useState } from 'react';
import { Package, Plus, UserRound, Wrench } from 'lucide-react';
import { api, post } from '../../api';
import EmptyState from '../../components/common/EmptyState';

export default function OperationsPage({ type }) { const [items, setItems] = useState([]); const [message, setMessage] = useState(''); const [form, setForm] = useState({ name: '', email: '', password: 'Welcome@123', role: 'mechanic', specialization: 'General Service', partNumber: '', quantity: 10, sellingPrice: 0, description: '', price: 0, category: 'Maintenance' }); const endpoint = type === 'team' ? '/users' : type === 'parts' ? '/parts' : '/services'; const title = type === 'team' ? 'Team & access' : type === 'parts' ? 'Parts & inventory' : 'Service catalog'; async function load() { setItems(await api(endpoint)); } useEffect(() => { load().catch((e) => setMessage(e.message)); }, [endpoint]); async function submit(e) { e.preventDefault(); try { const body = type === 'team' ? form : type === 'parts' ? { ...form, quantity: Number(form.quantity), sellingPrice: Number(form.sellingPrice) } : { name: form.name, description: form.description, category: form.category, price: Number(form.price) }; await post(endpoint, body); setMessage('Saved successfully'); setForm({ ...form, name: '', email: '', partNumber: '' }); load(); } catch (e) { setMessage(e.message); } } return <div className="page"><div className="page-heading"><div><p className="eyebrow orange">Operations</p><h1>{title}</h1><p className="muted">Keep the service center moving with clear, current records.</p></div></div><div className="ops-layout"><div className="panel"><div className="panel-heading"><h2>Add record</h2></div><form onSubmit={submit} className="form-grid">{type === 'team' ? <><label>Name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label><label>Email<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></label><label>Role<select value={form.role} onChange={(e) => setForm({ ...form, role: e.target.value })}><option>mechanic</option><option>staff</option><option>admin</option></select></label><label>Specialization<input value={form.specialization} onChange={(e) => setForm({ ...form, specialization: e.target.value })} /></label></> : <><label>Name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label><label>Category<input value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} /></label>{type === 'parts' ? <><label>Part number<input required value={form.partNumber} onChange={(e) => setForm({ ...form, partNumber: e.target.value })} /></label><label>Quantity<input type="number" min="0" value={form.quantity} onChange={(e) => setForm({ ...form, quantity: e.target.value })} /></label><label>Selling price<input type="number" min="0" value={form.sellingPrice} onChange={(e) => setForm({ ...form, sellingPrice: e.target.value })} /></label></> : <><label>Price<input required type="number" min="0" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} /></label><label className="wide-field">Description<textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} rows="3" /></label></>}</>}<button className="primary form-submit"><Plus size={17} />Save record</button></form>{message && <p className={message.includes('success') ? 'success' : 'error'}>{message}</p>}</div><div className="panel table-panel"><div className="panel-heading"><h2>Current records</h2><span className="eyebrow">{items.length} total</span></div>{items.length ? items.map((item) => <div className="record-row" key={item._id}><div className="record-icon">{type === 'team' ? <UserRound size={18} /> : type === 'parts' ? <Package size={18} /> : <Wrench size={18} />}</div><div><strong>{item.name}</strong><span>{type === 'team' ? `${item.role} · ${item.email}` : type === 'parts' ? `${item.partNumber} · ${item.quantity} in stock` : `₹${item.price?.toLocaleString()} · ${item.category}`}</span></div></div>) : <EmptyState icon={Package} text="No records yet" />}</div></div></div>; }
